{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Adresse list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Adresse1</th>
                <th>Adresse2</th>
                <th>Typeadresse</th>
                <th>Datecreateadresse</th>
                <th>Dateupdateadresse</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('adresse_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.adresse1 }}</td>
                <td>{{ entity.adresse2 }}</td>
                <td>{{ entity.typeadresse }}</td>
                <td>{% if entity.datecreateadresse %}{{ entity.datecreateadresse|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.dateupdateadresse %}{{ entity.dateupdateadresse|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('adresse_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('adresse_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('adresse_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
