{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CmdFac</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Adressidfac</th>
                <td>{{ entity.adressIdFac }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('cmdfac') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('cmdfac_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
