{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CategorieProduit</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity.type }}</td>
            </tr>
            <tr>
                <th>Datecreatecat</th>
                <td>{{ entity.datecreatecat|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Dateupdatecat</th>
                <td>{{ entity.dateupdatecat|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('categorieproduit') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('categorieproduit_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
